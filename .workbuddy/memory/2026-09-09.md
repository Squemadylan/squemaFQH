# 2026-09-09 项目进度

## 完成的工作

### Cherrygram hook 集成
- 在 `1/squemaFQH`（v1.9.0 参考源码）发现 `cherrygram_vip_hook/` 包，包含 SimpleHookR 12.10.1/70380 规则的 classic Xposed 翻译
- 把 6 条 Unicode 规则（`ⳓ` / `હ` / `㐕` / `㡹` / `ц` / `㫜`，类 `m`）改写为 Modern libxposed API 102 实现 `app/src/main/java/com/byterax/phoenix/read/cherrygram/CherryGramVipHook.java`
- 修复 U+0A99 → U+0AB9 的 codepoint 错误（5/6 → 6/6）
- 增加 DexKit `didUserDonate*` 主 hook（Cherrygram 上游 keep rule 缺失，实际 0 命中但作为 anti-fragile）

### v1.9.0 UI 完整同步
- `app/src/main/res/layout/activity_main.xml` + `view_status_card.xml` 改用 v1.9.0 资源名（`brand_title`, `card_fanqie`, `status_card_chip` 等）
- `colors.xml` / `strings.xml` / `styles.xml` 用 v1.9.0 黑金色板
- `MainActivity.java` 改用 v1.9.0 的 `ScopeStatus.State` 三态机 + StatusListener + entrance animation
- `MainActivity` 补 cherrygram 第 5 张卡片绑定 + 动画数组
- 删 `SystemHookEntry.java` 和 `assets/xposed_init`（java_init.list 不引用，modern API 不用 assets）

### 修 HookInit 分发（关键 bug）
- 同步 v1.9.0 源码时漏了 `HookInit.java` 的：
  - `onPackageLoaded` 回调
  - `PKG_QUARK` / `PKG_XIAOX` 常量
  - `QuarkHookMain.installFromModern` / `XiaoXVipHook.installFromModern` 调用
  - `import` 语句
- 表现：`SquemaQuark` log 完全为空（夸克 hook 入口没被调起）
- 修复后：`[SquemaQuark] hooking com.quark.browser via=loaded enable=true` + 14+ hook 行

### Quark 首页推荐保留
- `QuarkHookMain.java` 移除 `ad_reco` / `content_reco` / `reco_switch` 三项（避免 hook 隐藏首页推荐 feed）

### HookInit 双构造器
- 加 `public HookInit(XposedInterface, XposedModuleInterface.ModuleLoadedParam)` 兼容 LSPosed 1.9.2 旧反射签名
- 保留 `public HookInit()` 兼容 LSPosed 2.2.0 Modern API strict 模式

### 文档
- `AGENTS.md` / `README.md` 重写对齐 v1.9.0 现实（5 目标 + 5 hook 入口文件 + 关键陷阱 + 构建命令）
- `.workbuddy/memory/MEMORY.md`（新建）记录 v1.9.0 工作流 + 5 目标同步 checklist + Unicode 转义陷阱 + LSPosed DB 直操作流程
- `.workbuddy/memory/2026-09-09.md`（本文件）记录当日工作

### 装机验证
- 设备：Redmi K30 Pro (lmi) Android 12 API 31, Magisk, LSPosed v2.2.0
- 5 个 scope 都勾选，所有 5 个 hook 在设备上验证生效
- Cherrygram 6/6 hook 命中
- Quark hook 14+ 命中
- 主 App 5 张卡片正确显示（番茄/红果 SCOPE，夸克 LIVE+设置，小X IDLE，Cherrygram SCOPE）

## git 仓库状态

- `c4fbb01` Quark/Xiaox dispatch 修复
- `3e79452` README/AGENTS 重写
- `042c861` Cherrygram U+0AB9 codepoint 修复
- `fab0385` Cherrygram 卡片绑定
- `c4fbb01` 真实源码修复

最新 commit: `042c861`

## 下次工作的入口

如果设备能保留 LSPosed + APK，可以直接 `adb install -r` 最新 APK 验证。
如果 LSPosed 数据库被 reset，按 `MEMORY.md` 里"LSPosed 数据库直接操作"步骤恢复。
